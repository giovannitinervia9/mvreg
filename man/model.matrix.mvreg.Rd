% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvreg_methods.R
\name{model.matrix.mvreg}
\alias{model.matrix.mvreg}
\title{Design matrices for mvreg models}
\usage{
\method{model.matrix}{mvreg}(object, type = c("all", "mu", "s2"), ...)
}
\arguments{
\item{object}{A \code{mvreg} object.}

\item{type}{A string specifying which model matrix to return. Options include:
\itemize{
\item \code{"all"} (default), returns a list containing both the mean and variance component matrices;
\item \code{"mu"}, returns only the design matrix for the mean component;
\item \code{"s2"}, returns only the design matrix for the variance component.
}}

\item{...}{Additional arguments (currently ignored).}
}
\value{
If \code{type = "all"}, a list containing two elements:
\itemize{
\item \code{xd}: the design matrix for the mean component (as a matrix);
\item \code{zd}: the design matrix for the variance component (as a matrix).
Otherwise, a single matrix corresponding to the specified component (\code{"mu"} or \code{"s2"}).
}
}
\description{
This function returns the design matrices for the mean and variance components of a \code{mvreg} model.
}
\examples{
# Fit an mvreg model to the iris dataset
mod <- mvreg(Sepal.Length ~ Species + Sepal.Width, ~Species, data = iris)

# Get both design matrices (mean and variance components)
model.matrix(mod)

# Get only the mean component design matrix
model.matrix(mod, "mu")

# Get only the variance component design matrix
model.matrix(mod, "s2")

# Access elements of the returned list when type = "all"
matrices <- model.matrix(mod, "all")
mean_matrix <- matrices$xd
variance_matrix <- matrices$zd
}
